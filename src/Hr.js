import React from 'react'
import { View } from 'react-native'

const style = {
	borderBottomColor: '#cccccc',
	borderBottomWidth: 1,
	width: 330
}

export default function () {
	return (
		<View style={ style }/>
	)
}